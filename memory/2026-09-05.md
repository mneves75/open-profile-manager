# 2026-09-05

- Merged PR #47 as `cd7ceb9` after successful CI and CodeQL. Exact-main CI and CodeQL also passed; CI generated the release SPDX SBOM. GitHub Pages deployed and live HTML/CSS/JS matched source bytes. Dependabot returned zero open alerts.
- Published immutable `v0.1.7-beta1`, then immutable latest-stable `v0.1.7`, both from the same source commit. App version is 0.1.7/build9 on arm64 and x86_64. Verified beta did not replace the previous stable release.
- Both release scripts passed all 59 Swift tests, complete checks, strict Gitleaks/TruffleHog scans, signing, Apple notarization, stapling, Gatekeeper, dSYM matching, SBOM/checksums, GitHub asset attestations, and downloaded PTY/native execution. Distribution evidence is summarized in PROJECT_STATUS.md.
- Installed the downloaded production app and CLI with the existing atomic replacement helper. Signature checks and installed packaged smoke passed; process-to-window sample 337.854ms at load 5.67/6.26/6.12. No authentication data was read or migrated.
- Extracting the public ZIP in synced Documents introduced FinderInfo/file-provider metadata and failed codesign; the same checksum-verified ZIP extracted in nonsynced .scratch passed signature, staple and Gatekeeper without modifying its contents. Public ZIP was intact.
- Updated release status and security evidence; opened the 0.1.8 Unreleased changelog section without changing the released app version.
- The user requested another version bump and beta/production publication after the installation note. Prepared maintenance candidate 0.1.8/build10; app behavior is unchanged. AGENTS.md and CLAUDE.md remain current and require no duplicated version state.
- Merged PR #49 as `37b056e`; published immutable beta1 and latest stable 0.1.8/build10. All 59 tests, full gates, strict scans, native independent P3 review, and exact-source CI/CodeQL passed. CLI autoreview could not obtain a Codex verdict and its Claude attempt hit a session limit; no CLI review success is claimed.
- Removed unnecessary Apple submission UUIDs from public documentation after a confirmed false-positive npm-token classification. Strict scanning then passed unchanged with zero candidates; no exceptions added.
- Both 0.1.8 releases passed notarization, signing, stapling, Gatekeeper, dSYM/SBOM/checksums and downloaded smoke. The beta script stopped after publication on an initially unavailable attestation; a short wait and explicit verification of all four assets completed that gate before production. Production's wrapper completed successfully.
- Installed the public 0.1.8 app and CLI; signatures and packaged PTY/window smoke passed. Interactive desktop inspection/reopening was blocked by the locked Mac. Opened 0.1.9 Unreleased without changing the shipped app version.
