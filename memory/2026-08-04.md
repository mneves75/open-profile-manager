# 2026-08-04

- Reproduced the interactive launch failure under a real PTY: Foundation `Process` changed PID/process group and Codex lost the foreground terminal, stopping on input.
- Added a deterministic PTY regression for `run`, `login`, and `logout`; switched those commands to direct `execve` while retaining `Process` for desktop launch and app-server status.
- Moved bounded status concurrency into `ProfileCore`; CLI and GUI now share the same ordered batch implementation. A two-process barrier test proves overlap.
- Stable Xcode 26.6 checks and security scans pass with 54 tests; Xcode 27 beta also builds and passes all 54 tests.
- Release benchmark median for two-profile `status --all`: 1,642 ms before, 788 ms after, measured at load averages 5.63/6.20/7.09.
- PR #7 passed build/test/lint, dependency review, secret scan, and CodeQL, then squash-merged to `main` as `c5754b1`.
- Published immutable GitHub Release `v0.1.1` from `c5754b1`. Apple notarization accepted submission `5e5f7581-f245-432d-80b5-0abe2a85d473`; stapling, Gatekeeper, dSYMs, SBOM, checksums, attestations, redownload, and isolated-home launch passed.
- Added uncommitted Remotion sources under `video/` for a 38-second launch clip and an 85-second tutorial. Both synthetic-data-only H.264/AAC renders, native-resolution frame review, and video lint/type checks passed; outputs remain ignored under `video/out/`.
- Added a 72-second GUI tutorial and retuned the generated UI click into a deeper bass thock; rendered `video/out/gui-tutorial.mp4` and checked key frames at native resolution.
- Reimagined the native app's graphite UI and prepared a responsive GitHub Pages product site with a generated hero, all three videos, captions, and a SHA-pinned deployment workflow.
