# 2026-08-03

- Started the localization pass as 0.2.0 from clean `main` at `43c65ab`; baseline `Scripts/check.sh` passed 49 tests in 6 suites. Before merge, the work was consolidated into marketing version 0.1.0 build 2 at the user's request.
- Confirmed this is a native macOS Swift 6/SwiftUI/AppKit package with no mobile or web target, so Argent workflows do not apply.
- Chose the native bundle contract: `en-US` is the source and fallback, `pt-BR` is the alternate localization, and macOS chooses automatically from the user's preferred languages. The stable `opm` CLI/JSON contract remains English.
- Found that the initial catalog claimed pt-BR support but used generic `en`, did not test completeness or fallback, and allowed dynamic ProfileCore error strings to bypass GUI localization.
- Centralized GUI strings and typed errors behind `L10n`, preserving user data and stable CLI/protocol values verbatim; the catalog now has 131 completed keys per locale.
- Added a blocking localization checker for source coverage, locale completeness, and exact supported format directives, plus two focused error-interpolation tests; `Scripts/check.sh` now passes 51 tests.
- Packaged with an Apple Development identity and rendered isolated main/editor windows in `en-US` and `pt-BR`; system pt-BR selected automatically, and an unsupported language resolved to `en-US`.
- Full security review found no confirmed findings: local/full-history secret scans were clean, the app has no network routes, and GitHub reported zero open Dependabot, CodeQL, or secret-scanning alerts.
- `gpt-5.6-sol/high` autoreview found a frozen relative reset label, a placeholder parser that ignored unknown `%` directives, and English-only editor prompts. All were fixed with a minute timeline, fail-closed directive parsing, and catalog-backed prompts; the final full rerun was clean.
