# 2026-09-04

- Completed source audit across Swift core/CLI/native tests, scripts/CI/rules, website and video sources. Retained descriptor-relative I/O, ACL/ownership checks, writer lock, bounds, strict scalar parsing and meaningful regression tests.
- Removed 4,991 CSS/JS bytes; all 190 DOM elements (including pseudo styles) match across eight desktop/mobile states. Three sampled Remotion PNGs match byte-for-byte. Raw proof remains ignored under `.scratch/slop-web-proof`.
- Alternating warm web checks with the same patched dependency tree: median 5.377s before vs 1.999s after removing repeated npm ci (three runs each; host load recorded). This is verification latency, not product startup/LCP.
- Scripts/check.sh passes under Node24.20/npm11: 59 Swift tests plus lint, localization, script/PTY, site/video and dependency gates. Default Node26.7 correctly fails the runtime requirement; no global runtime switch was made.
- PTY gate previously accepted forced exit7; new wait assertion rejects that mutant, and normal tests prove intentional exit7 propagation.
- fast-uri3.1.5 accepted malformed IPv6;3.1.6 rejects it while valid IPv6 still parses. npm audit is clean. No other lockfile package changed.
- Debug package signing and isolated visible-window smoke pass. Native synthetic-home QA passed add/edit, disabled relative-path save, duplicate-ID error and removal cancellation. No real authentication, desktop launch or Finder installation was tested.
- Actual bootstrap execution exposed redundant blanket chmod changing a sourced library's Git mode; deleted the chmod, restored the mode and reran bootstrap/shellcheck. Fresh independent verification and P3 autoreview found no actionable issues. Changes remain local/uncommitted.
- User requested deployment to this Mac. Built with stable Xcode using install-local.sh and installed the Apple Development-signed Release build to ~/Applications/Open Profile Manager.app and CLI to ~/.local/bin/opm. Previous installation backed up in .scratch/local-install-backup-xks1xbor. Installed signature, build-byte parity, version and packaged PTY/window smoke passed; installed app opened with existing profiles. Local deployment only; no commit, push, notarization or public release.
- User authorized full beta/production release. Prepared 0.1.7/build9 and numbered GitHub beta support (no TestFlight target). Full candidate checks passed; generated Remotion comparison bundles triggered vendor-search-key detections, so retained them outside source and reran unchanged strict scanners. No secret values or bypass rules added.
