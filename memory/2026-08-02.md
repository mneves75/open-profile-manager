# 2026-08-02

- Created the repository and selected a Swift-only architecture with a shared deep module for CLI and GUI behavior.
- Set the public safety boundary: explicit profile selection and read-only quota visibility; no automatic rotation or credential handling.
- Pinned `swift-argument-parser` 1.8.2 after checking the current official Apple release and repository health.
- Set macOS 15 as the deployment minimum and Swift 6 language mode as mandatory.
- Implemented and installed the shared core, CLI, native SwiftUI app, and shell-free Finder launchers.
- A live status test found that closing app-server stdin caused premature shutdown; kept the transport open through account/rate-limit responses and verified two isolated test profiles without retaining their identifiers.
- Completed the security audit and fixed registry writer races, unbounded inputs, control characters, unsafe directory permissions/symlinks, and launcher executable/signing validation.
- Verified 49 tests in 6 suites, local development signing, a live native UI window, and two official-app processes with distinct profile data directories.
- Developer ID signing and notarization remain unavailable; source publication and local development-signed use remain available.
- Autoreview found and drove fixes for fresh-checkout icon generation, Finder-safe executable discovery, symmetric registry size checks, execution-time directory revalidation, exact owner permissions, no-auth provider status, and the explicit `opm status --all` contract.
- A later review fixed explicit `CODEX_HOME` forwarding through LaunchServices, read-time registry ownership/mode validation, and packaged-app localization resources; native-window presence was verified without committing screenshots or account data.
- Final editor review rejected relative path rebasing and kept form state visible after validation or persistence errors; a focused path-parser regression test was added.
- Final security/robustness review added macOS ACL enforcement, four-process status concurrency, automatic GUI-directory doctor coverage, and staged atomic local replacement. A deliberately failed upgrade preserved the existing install; the corrected live upgrade then passed signature and version checks.
- The next review closed Darwin `mktemp` uniqueness, nonzero GUI launch reporting, late status-output overflow, and false-pass diagnostics for absent paths beneath symlinks; all three new regressions are covered.
- The following review bounded JSONL record amplification, made launcher bundle IDs injective, rejected executable directories while preserving normal symlinks, and skipped malformed `.app` bundles. Live `doctor` resolved the real symlinked Codex binary and both launchers migrated to distinct IDs.
- The next security review found that global `CODEX_ACCESS_TOKEN`, `CODEX_API_KEY`, or `CODEX_SQLITE_HOME` could override a selected profile. Launch and status now remove those official conflicting overrides while keeping provider variables; standardized root paths are rejected and the GUI picker no longer creates predictably unsafe-mode folders.
- Persistence, remaining core, and integrated source groups were reviewed separately through an isolated reviewer profile until no accepted or actionable findings remained; reviewer account details are intentionally omitted.
- The last security pass replaced path-based private-file operations with descriptor-relative traversal and I/O, hardened ancestor ownership/mode/ACL validation, isolated registry locks, and rejected overlapping or derived profile storage.
- Integrated review then closed physical-path alias and persisted-registry isolation bypasses and added official-app discovery under the per-user Applications directory.
- The next integrated pass caught APFS firmlink identity and a custom-support-directory doctor mismatch; storage comparison now uses `F_GETPATH_NOFIRMLINK`, and doctor derives its GUI path from the registry's canonical configuration.
- Authentication-isolation review caught the standard `OPENAI_API_KEY` override; it now joins the exact process-global denylist and its hostile-environment regression.
- Launcher review required private destinations and explicit bundle modes, bounded/nonblocking application and managed-launcher plist reads, and fail-closed handling for dangling symlink collisions.
- App-server writes now set Darwin `F_SETNOSIGPIPE` on the pipe descriptor so a child that exits between requests yields a typed unavailable status instead of terminating `opm`.
- Public-release review removed profile labels, reviewer-account details, machine-specific evidence, regional metadata, personal commit email, and real-name attribution before the first commit; community health, private-reporting, ownership, and hardened Actions metadata were added.
- Created the public `mneves75/open-profile-manager` GitHub repository for the reviewed 0.1.0 source; no binary release or tag was published because Developer ID notarization remains unavailable.
- The first public CodeQL run found cleartext account-email output in human-readable status; default output now omits the email while explicit structured JSON retains its documented field.
